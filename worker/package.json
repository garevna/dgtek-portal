{
  "name": "portal-worker",
  "version": "1.0.0",
  "scripts": {
    "worker-dev": "yarn add crypto-js && webpack --config webpack.config.worker.js --mode development -w && yarn remove crypto-js",
    "worker-prod": "yarn add crypto-js && webpack --stats verbose --config webpack.config.worker.js --mode production && yarn remove crypto-js"
  },
  "dependencies": {
    "@babel/polyfill": "^7.12.1"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.13.12",
    "babel-core": "^6.26.3",
    "babel-polyfill": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-0": "^6.24.1",
    "webpack": "^5.31.2"
  }
}
